Applied, in order.

  decisions/brd-decisions.md — new 16 Sep entry: the rule verbatim, the two supersessions (two-human model; analyst's-own-credentials), why the credentials split, both prerequisites, the closed open point.

  brd/01-capability-catalogue.md — eleven places: preamble; usage-summary rows 4 and 9; skill 4 "What it does" and section (b) renamed "The approval half: code as maker, human as checker" with the rule, the
  segregation-of-duties reasoning and the two prerequisites; the "checkpoint regardless" line; the slide 15 framing; skill 5 "What it does" and its standing-rule closer; a new paragraph in skill 7 on the
  manual front-office check; skill 9 sub-mode (c) and its narrowed open point; the approval-orchestration row.

  brd/annex-scope-register.md — the "As-is checker drawn" column meaning: exactly one human decision point in the to-be, column records as-is evidence only.

  brd/solution-overview.html — Panel 1 rebuilt: one hexagon (the checker) replaces the hexagon and the diamond, stage 6 is the maker write into the pending state, outcome becomes "Change committed and logged",
  new dashed note carries the rule and the comparison-table requirement, EDM vocabulary throughout. Also fixed a defect the previous version had: the query loop crossed the control lane text.

  log/2026-09-16-session-2.md — session entry.

  Three things to flag:

  1. CLAUDE.md is untouched as instructed. The two lines it needs to supersede are the design principle "all writes execute on the analyst's own credentials" and the Validate and Approve bullet naming "maker
     confirmation, checker".
  2. Skill 7 gained a consequence you had not raised. The Phase 1 manual front-office check is human work, so for the five closure units the read is done by an analyst. I resolved it as: the analyst reads and
     records evidence but never writes, and the analyst who performs the read is not the checker who approves the closure (same segregation-of-duties ground). Phase 2 removes the step entirely. Tell me if you
     want that stated differently.
  3. The HTML has not been rendered. Layout was verified by arithmetic against 1920 x 1080 and the 12 px floor; the hexagon text fits at every y I checked. It needs your eye at 100%.

✻ S
