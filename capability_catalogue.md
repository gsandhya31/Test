Re-read CLAUDE.md; the chapter template and the caveat rule have changed. Restructure brd/ch-01-confirmation-contacts-gtype.md in place to the new template, then complete it:

1. Section 1: three-sentence overview and one activity table only. Merge the old sections 1 and 4; delete the duplicated scope and status tables.
2. Section 2: one sub-section per flow group (G1 activities 1.1 and 1.2, G2 activity 1.3, G3 activities 1.4 to 1.7), each with As-is (input, frequency, checker today, numbered steps of 6 to 10 lines), To-be (configuration shape with skill and sub-mode tags, the checker's extraction gate and commit approval marked, rejection paths, configurable checks with "content: to be supplied by <owner>"), What changes for the team (three lines), and Open points for this activity.
3. Sections 3 to 6 at chapter level: FR table traced to to-be steps; controls and governance once (code as maker, human as checker, automation identity, comparison table, audit; governance mappings quoted verbatim with article numbers); data, systems and prerequisites (webform API, per-system pending state, ORM acceptance); open points roll-up by owner.
4. Language: plain, present tense, no narration of sources in body text. Every source conflict, pending sign-off note, slide or journal reference goes inside a [CAVEAT: ...] tag so the business version can be produced by stripping the tags. Say each fact once; if the register already holds it, reference the register rather than repeating the table.
Do it in two stops: sections 1 and 2 first, tell me when ready; then sections 3 to 6.
