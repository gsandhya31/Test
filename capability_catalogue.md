
Create brd/solution-overview.html: a single self-contained page (inline SVG and CSS, no external scripts, fonts or images; must open offline on a corporate laptop) showing the high-level to-be solution for senior stakeholders. Content:

Panel 1, the flow, left to right: request receipt (IP1 webform link, IP2 Excel attachment, IP3 free-text email, IP4 EVE work item) -> Ingestion -> Unstructured to Structured and Transform and Enrich -> Validate and Approve (a) rules -> Compare and Match -> analyst confirmation (HITL) -> Calculate and Process (writes to EVE and Totoro on the analyst's credentials) -> checker (HITL) -> Route and Distribute, Report and Notify and Monitor and Control for queries, approvals, pending states and audit. Show the two human decision points as distinct shapes. Show the query loop returning to the requestor. Label each box with the skill name; mark Unstructured to Structured and Compare and Match as "registered utility (reused)" and the other seven as "new NTI". Mark the Phase 1 boundary: the position and alt-code check against other FO systems is a manual analyst step in Phase 1, automated in Phase 2.

Panel 2, scope in one glance: the four Level 2 areas with Phase 1 activity counts (8, 12, 13, 0), Phase 2 (13) and out of scope (5), and the sentence that every Phase 1 activity maps to one configuration record on the platform (22 configuration records across 19 design units).

Panel 3, a compact table: the nine skills, one line each: what it does in one sentence, how many Phase 1 units use it, registered or new NTI.

Wording rules from CLAUDE.md apply: no vendor names, the AI stack is "Chinou API", no em-dashes, Phase 1 / Phase 2 labels. Landscape layout, one screen at 1920 x 1080, fonts no smaller than 12 px, so it transfers to a slide. Do not print the file; tell me the path.
