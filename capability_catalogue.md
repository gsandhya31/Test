Change intake, decision by SG, 17 Sep. Each configuration has two human decision points: (1) an extraction confirmation gate, where the analyst confirms the extracted and derived fields before any processing or staging; on by default for every configuration in Phase 1, switchable off per configuration by a wizard flag once extraction quality is proven; (2) the checker, mandatory and never switchable, who sees the comparison table and approves or rejects on their own credentials, which commits the change. The automation remains the maker. Supersedes "exactly one human decision point". Log it, tell me where it lands in the catalogue and register, and apply to the catalogue (Validate and Approve gains sub-mode (b1) extraction confirmation, (b2) checker approval). I am updating CLAUDE.md myself. Then wait for my Step 2 instruction.
