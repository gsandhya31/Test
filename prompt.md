Corrections and decisions on Parts B to E. Apply to the working files under log/session-1/, regenerate partD-scope-register-draft.md, then stop.

Naming
- Activity names in every file: the status-sheet name verbatim. Where the slide title differs, add it in an "alias" column. Do not rename activities.

Taxonomy
- Add IP4: request originates as an EVE workflow / EVERequest item, no email. "Automation starts at email receipt" becomes "at request receipt (email or EVE work item)". U1 applies to email channels only; for IP4, U2 reads request data from EVE.
- 3.1, 3.8, 3.10, 3.15: tag as drawn (email pattern plus IP4), trigger marked [BUSINESS INPUT] for verification. 3.8 KYC-team trigger: assume IP3, [BUSINESS INPUT]. 2.13: no channel stated, open point.
- Slide utility tags are as-is annotations only; derive to-be utilities from the steps.

Scope rules
- A to-be is drafted wherever a documented as-is exists and the activity is not Phase 2 or out of scope. Pending sign-off does not block the to-be; the chapter marks it "to-be drafted on an as-is pending business sign-off". Applies to 2.7 both regions, 2.10, 2.13, 3.2, 3.6, 3.8 all regions, 3.15, 3.17. Supersedes the earlier rule.
- 2.3: covered elsewhere, no to-be. Record slide 17 (EVE RDM Bulk Loader) as an as-is note in chapter 2 section 2, ask Rupesh which activity it belongs to and whether the 2.3 status cell is a copy-paste from 2.14.
- 3.7: covered elsewhere, points to 2.6.
- 3.4: two register rows only: Totoro (Phase 1) and other FO systems (Phase 2, systems as named on the sheet). Drop the Sphinx/Falcon question.
- 4.3 / 4.4: the status sheet is authoritative; correct flows-area-4.md.
- Chapter 4: no template sections; one note that 4.1 to 4.9 are Phase 2 (third-party integration) plus the activity list.

Structure
- Sections 2 and 5: one sub-section per flow group, not per activity, with a mapping table at the head of section 2. Chapter 1 groups: {1.1, 1.2}, {1.3}, {1.4 to 1.7}.
- Regional variants: per-region rows in the register; region sub-sections in sections 2 and 5 (2.7, 3.8).
- Cross-area duplicates: write once in the earlier chapter, cross-reference from the later one. 3.2 -> 2.2, 3.16 -> 2.15, 3.6 reuses 2.4 structure, 3.11 twin of 2.12. Catalogue records these as one parameterised configuration (org / account), not two.
- 3.15: all gaps as open points for business; slide 50 "account selection logic unknown" named as a build blocker.

Register
- Keep "as-is checker drawn" as a register column. [BUSINESS INPUT] tables carry frequency and volumes only.
- Register order: area 1, 2, 3, 4, matching chapter order.

Journal: log all of the above as one dated entry, with the to-be rule and IP4 as superseding entries.
